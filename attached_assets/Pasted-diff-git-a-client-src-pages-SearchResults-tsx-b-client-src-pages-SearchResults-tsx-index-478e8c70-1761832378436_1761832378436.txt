diff --git a/client/src/pages/SearchResults.tsx b/client/src/pages/SearchResults.tsx
index 478e8c702dafc7d06df6a33c1a077d43972800c5..f4838b5e9f956ea40b88d4d7ccb3166af465b4eb 100644
--- a/client/src/pages/SearchResults.tsx
+++ b/client/src/pages/SearchResults.tsx
@@ -1,64 +1,163 @@
+import { useEffect, useMemo } from "react";
 import { Header } from "@/components/Header";
 import { CompanyCard } from "@/components/CompanyCard";
+import { NcmCard } from "@/components/NcmCard";
+import { Card } from "@/components/ui/card";
 import { useLocation } from "wouter";
 import { useQuery } from "@tanstack/react-query";
+import { useToast } from "@/hooks/use-toast";
+import { useAuth } from "@/hooks/useAuth";
 
 interface Company {
   id: number;
   name: string;
   kind: 'importer' | 'exporter';
   countryCode: string;
 }
 
+interface CompanySearchResponse {
+  results: Array<{ company: Company; score: number; allowed: boolean; reason?: string; remainingSlots: number }>;
+}
+
+interface NcmSearchResponse {
+  results: Array<{ code: string; description: string | null; totalShipments: number; allowed: boolean; reason?: string; remainingSlots: number }>;
+}
+
 export default function SearchResults() {
-  const [, setLocation] = useLocation();
-  const searchParams = new URLSearchParams(window.location.search);
-  const query = searchParams.get('q') || '';
-  
-  const { data: results = [], isLoading } = useQuery<Array<{ company: Company; score: number }>>({
+  const [currentLocation, setLocation] = useLocation();
+  const { toast } = useToast();
+  const { user, isLoading } = useAuth();
+  const query = useMemo(() => {
+    const searchIndex = currentLocation.indexOf('?');
+    const search = searchIndex >= 0 ? currentLocation.slice(searchIndex) : window.location.search;
+    return new URLSearchParams(search).get('q') || '';
+  }, [currentLocation]);
+
+  useEffect(() => {
+    if (!isLoading && !user) {
+      setLocation(`/login?redirect=${encodeURIComponent(window.location.pathname + window.location.search)}`);
+    }
+  }, [user, isLoading, setLocation]);
+
+  const companiesQuery = useQuery<CompanySearchResponse>({
     queryKey: [`/api/companies/search?q=${encodeURIComponent(query)}`],
-    enabled: !!query,
+    enabled: !!query && !!user,
+  });
+
+  const ncmQuery = useQuery<NcmSearchResponse>({
+    queryKey: [`/api/ncm/search?q=${encodeURIComponent(query)}`],
+    enabled: !!query && !!user,
   });
 
+  const companies = companiesQuery.data?.results ?? [];
+  const ncms = ncmQuery.data?.results ?? [];
+
+  const isFetching = companiesQuery.isLoading || ncmQuery.isLoading;
+  const hasResults = companies.length > 0 || ncms.length > 0;
+
+  const handleCompanyClick = (result: CompanySearchResponse["results"][number]) => {
+    if (!result.allowed) {
+      toast({
+        title: "Plano insuficiente",
+        description: result.reason || "Amplie sua franquia em planos para consultar este registro.",
+        variant: "destructive",
+      });
+      return;
+    }
+    setLocation(`/company/${result.company.id}`);
+  };
+
+  const handleNcmClick = (result: NcmSearchResponse["results"][number]) => {
+    if (!result.allowed) {
+      toast({
+        title: "Plano insuficiente",
+        description: result.reason || "Amplie sua franquia em planos para consultar este NCM.",
+        variant: "destructive",
+      });
+      return;
+    }
+    setLocation(`/ncm/${encodeURIComponent(result.code)}`);
+  };
+
+  useEffect(() => {
+    if (companiesQuery.error instanceof Error && companiesQuery.error.message.startsWith("401")) {
+      setLocation(`/login?redirect=${encodeURIComponent(window.location.pathname + window.location.search)}`);
+    }
+  }, [companiesQuery.error, setLocation]);
+
+  if (isLoading || !user) {
+    return (
+      <div className="min-h-screen bg-background">
+        <Header compact onSearch={(q) => setLocation(`/search?q=${encodeURIComponent(q)}`)} />
+        <div className="max-w-4xl mx-auto px-4 md:px-6 lg:px-8 py-12">
+          <p className="text-muted-foreground">Carregando resultados da busca...</p>
+        </div>
+      </div>
+    );
+  }
+
   return (
     <div className="min-h-screen bg-background">
-      <Header compact onSearch={(q) => console.log('Search:', q)} />
-      
-      <div className="max-w-4xl mx-auto px-4 md:px-6 lg:px-8 py-12">
-        <div className="mb-8">
-          <h1 className="text-3xl font-bold mb-2" data-testid="text-search-results-title">
+      <Header compact onSearch={(q) => setLocation(`/search?q=${encodeURIComponent(q)}`)} />
+
+      <div className="max-w-4xl mx-auto px-4 md:px-6 lg:px-8 py-12 space-y-10">
+        <div className="space-y-2">
+          <h1 className="text-3xl font-bold" data-testid="text-search-results-title">
             Resultados da Busca
           </h1>
-          {isLoading ? (
-            <p className="text-muted-foreground">Buscando...</p>
-          ) : (
-            <p className="text-muted-foreground" data-testid="text-results-count">
-              {results.length} empresas encontradas
-            </p>
-          )}
+          <p className="text-muted-foreground" data-testid="text-results-count">
+            {isFetching ? 'Buscando...' : `Consultas encontradas para "${query}"`}
+          </p>
         </div>
 
-        <div className="space-y-4">
-          {isLoading ? (
-            <div className="text-center py-12 text-muted-foreground">
-              Carregando resultados...
-            </div>
-          ) : results.length === 0 ? (
-            <div className="text-center py-12 text-muted-foreground">
-              Nenhuma empresa encontrada para "{query}"
-            </div>
-          ) : (
-            results.map(({ company, score }) => (
-              <CompanyCard
-                key={company.id}
-                {...company}
-                score={score}
-                onClick={() => setLocation(`/company/${company.id}`)}
-              />
-            ))
-          )}
-        </div>
+        {isFetching ? (
+          <div className="text-center py-12 text-muted-foreground">Carregando resultados...</div>
+        ) : !hasResults ? (
+          <Card className="p-8 text-center text-muted-foreground">
+            Nenhum importador, exportador ou NCM foi localizado para "{query}". Ajuste seu termo ou entre em contato
+            com nossa equipe para ampliar a base de dados.
+          </Card>
+        ) : (
+          <div className="space-y-8">
+            {companies.length > 0 && (
+              <section className="space-y-4">
+                <h2 className="text-xl font-semibold">Importadores e Exportadores</h2>
+                <div className="space-y-3">
+                  {companies.map((result) => (
+                    <CompanyCard
+                      key={result.company.id}
+                      {...result.company}
+                      score={result.score}
+                      allowed={result.allowed}
+                      lockReason={result.reason}
+                      onClick={() => handleCompanyClick(result)}
+                    />
+                  ))}
+                </div>
+              </section>
+            )}
+
+            {ncms.length > 0 && (
+              <section className="space-y-4">
+                <h2 className="text-xl font-semibold">NCMs</h2>
+                <div className="space-y-3">
+                  {ncms.map((result) => (
+                    <NcmCard
+                      key={result.code}
+                      code={result.code}
+                      description={result.description}
+                      totalShipments={result.totalShipments}
+                      allowed={result.allowed}
+                      lockReason={result.reason}
+                      onClick={() => handleNcmClick(result)}
+                    />
+                  ))}
+                </div>
+              </section>
+            )}
+          </div>
+        )}
       </div>
     </div>
   );
 }
